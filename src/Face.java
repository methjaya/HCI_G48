public class Face {
    int[] vertexIndices;
    int[] textureCoordIndices;

    public Face(int[] vertexIndices, int[] textureCoordIndices) {
        this.vertexIndices = vertexIndices;
        this.textureCoordIndices = textureCoordIndices;
    }
}